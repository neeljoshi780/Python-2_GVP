(3).Print a list recursively.

def printListRecursively(lis):
    for item in lis:
        if type(item)==list:
            printListRecursively(item)
        else:
            print(item)


lis = [1,[2,[3,4],5],6,7]
printListRecursively(lis)
